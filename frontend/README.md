# Auto Elite - Frontend (React + Vite + Tailwind)

## Setup
1) `npm install`
2) Copie `.env.example` para `.env` e ajuste `VITE_API_URL` para o backend.
3) `npm run dev`

## Build
- `npm run build` e `npm run preview`
